<div class="block">
  <div class="secondary-navigation">
    <ul class="wat-cf">
      <li class="first"><%= link_to "Conta " +  @account.name, account_path(@account) %></li>
      <li><%= link_to "Vaga #{@job.name}", account_job_path(@account, @job)  %></li>
      <li class="last active"><%= link_to "Criar Anuncio", "#" %></li>
    </ul>
  </div>          
  <div class="content">            
    <h2 class="title"><%= t("web-app-theme.new", :default => "New")%>  Listing</h2>
    <div class="inner">
      <% form_for :listing, :url => account_job_listings_path(@account, @job), :html => { :class => :form } do |f| -%>
        <%= render :partial => "form", :locals => {:f => f} %>
      <% end -%>
    </div>
  </div>
</div>

<% content_for :sidebar, render(:partial => 'sidebar') -%>
