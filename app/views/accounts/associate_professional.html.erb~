<div class="block">
  <div class="secondary-navigation">
    <ul class="wat-cf">
      <li class="first"><%= link_to t("activerecord.models.account") + " " + eval("@account.name"), account_path(@account) %></li>
      <li class="last active"><%= link_to "Associar a Conta", "#" %></li>
    </ul>
  </div>
  <div class="content">
    <h2 class="title"></h2>
    <div class="inner">

      <% form_for :account, :url => account_associate_account_path(@account), :html => { :class => :form, :method => "put" } do |f| -%>

	  <div class="group">
	    <%= f.label :login_associate, t("activerecord.attributes.account.login", :default => "Login"), :class => :label %>
	    <%= f.text_field :login_associate, :class => 'text_field' %>
	  </div>

	  <div class="group">
	    <%= f.label :email_main_associate, t("activerecord.attributes.account.email_main", :default => "Email main"), :class => :label %>
	    <%= f.text_field :email_main_associate, :class => 'text_field' %>
	  </div>
        
	<div class="group navform wat-cf">
	  <button class="button" type="submit">
	    <%= image_tag("web-app-theme/tick.png", :alt => "#{t("web-app-theme.save", :default => "Save")}") %> <%= t("web-app-theme.save", :default => "Save") %>
	  </button>  
	  <%= link_to "#{image_tag("web-app-theme/cross.png", :alt => "#{t("web-app-theme.cancel", :default => "Cancel")}")} #{t("web-app-theme.cancel", :default => "Cancel")}", account_path(@account), :class => "button" %> 
	</div>
      <% end -%>

    </div>
  </div>
</div>

<% content_for :sidebar, render(:partial => 'layouts/sidebar', :locals => {:model => "account", :menu_visible => true }) -%>
